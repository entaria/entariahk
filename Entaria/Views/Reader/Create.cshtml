@model Entaria.Models.Reader

@{
    ViewBag.Title = "Create";
}

<h2>Create</h2>

@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>Reader</legend>

        <div class="editor-label">
            @Html.LabelFor(model => model.LocationId)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.LocationId)
            @Html.ValidationMessageFor(model => model.LocationId)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.HardwareIdentifier)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.HardwareIdentifier)
            @Html.ValidationMessageFor(model => model.HardwareIdentifier)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.AttachedToNote)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.AttachedToNote)
            @Html.ValidationMessageFor(model => model.AttachedToNote)
        </div>

        <p>
            <input type="submit" value="Create" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
