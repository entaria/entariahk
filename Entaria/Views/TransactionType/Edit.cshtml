@model Entaria.Models.TransactionType

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>

@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>TransactionType</legend>

        @Html.HiddenFor(model => model.TransactionTypeId)

        <div class="editor-label">
            @Html.LabelFor(model => model.TransactionTypeName)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.TransactionTypeName)
            @Html.ValidationMessageFor(model => model.TransactionTypeName)
        </div>

        <p>
            <input type="submit" value="Save" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
